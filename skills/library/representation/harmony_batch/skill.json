{
  "skill_id": "harmony_batch",
  "capability": "representation",
  "execution_layer": {
    "required_inputs": [
      "input_data",
      "params_dict"
    ],
    "default_params": {
      "key": "batch",
      "max_iter_harmony": 10,
      "theta": 2.0,
      "lambda_": 0.1,
      "sigma": 0.1
    },
    "output_objects": [
      "harmony_adata"
    ],
    "script": "run.py"
  },
  "cognitive_layer": {
    "purpose": "Harmony corrects batch effects by softly clustering cells across batches and adjusting coordinates to maximize cell diversity while minimizing batch-specific variation. Unlike methods that completely remove batch effects, Harmony preserves biological variation while accounting for technical artifacts.",
    "parameter_impact": {
      "theta": "Diversity clustering penalty. Higher values allow more diversity within clusters, providing stricter batch correction. Lower values keep cells more similar within batches.",
      "lambda_": "Diversity scaling penalty. Higher values result in more aggressive batch correction by scaling down batch-specific variation more strongly."
    }
  },
  "critic_layer": {
    "metrics_to_extract": [
      "batch_key",
      "n_batches",
      "n_pcs",
      "harmony_converged"
    ],
    "success_thresholds": "harmony_converged == True and n_batches >= 1",
    "context_params": [],
    "error_handling": {
      "KeyError: batch key not found": "Ensure the batch column exists in adata.obs. Common keys: 'batch', 'stim', 'donor', 'sample'.",
      "ValueError: X_pca not found": "Run sc.tl.pca() before Harmony batch correction."
    },
    "script": "critic.py"
  },
  "parameter_science_guide": {
    "theta": {
      "overcorrection": {
        "adjust": "decrease theta by 0.5-1.0",
        "causal_chain": "Lower theta reduces the diversity penalty, allowing more batch-specific variation to remain, which preserves subtle biological differences.",
        "expected_effect": "Less aggressive batch correction, more biological variation preserved"
      },
      "undercorrection": {
        "adjust": "increase theta by 0.5-1.0",
        "causal_chain": "Higher theta increases the diversity penalty, forcing clusters to be more mixed across batches, resulting in stronger batch correction.",
        "expected_effect": "More aggressive batch correction"
      }
    }
  }
}
